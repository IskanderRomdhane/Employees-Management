spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
springdoc:
  default-produces-media-type: application/json
server:
  port: 8088
  servlet:
    context-path: /api/v1/

keycloak:
  server:
    url: http://localhost:9090/
  realm: Employees-Manager
  resource: emp
  admin:
    username: mohsen
    password: 123

